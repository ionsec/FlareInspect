services:
  - type: web
    name: flareinspect-web
    env: node
    plan: free
    buildCommand: npm ci
    startCommand: node web/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 3000
