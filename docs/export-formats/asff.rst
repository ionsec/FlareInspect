===========
ASFF Export
===========

The ASFF (AWS Security Finding Format) export produces findings compatible with AWS Security Hub for centralized security posture management.

Usage
-----

.. code-block:: bash

   flareinspect export -i assessment.json -f asff -o output.asff

