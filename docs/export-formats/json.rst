===========
JSON Export
===========
===========

The JSON export format produces a machine-readable, re-importable file containing the full assessment data including findings, scores, metadata, and compliance mappings.

Usage
-----

.. code-block:: bash

   flareinspect export -i assessment.json -f json -o output.json

