===========
OCSF Export
===========
===========

The OCSF (Open Cybersecurity Schema Framework) export normalizes findings into the OCSF schema for SIEM integration and cross-tool correlation.

Usage
-----

.. code-block:: bash

   flareinspect export -i assessment.json -f ocsf -o output.ocsf

